# Pull Request: Init project scaffold

This PR initializes the repository with a full project scaffold based on the Sunny Tea House AI 评价生成系统 requirements.

Includes:
- backend: FastAPI async backend with SQLAlchemy models, CRUD, auth, AI adapter placeholder, DB init script
- frontend: Vue 3 + Vite mobile H5 demo pages
- Dockerfile: multi-stage build (frontend -> backend)
- Alembic basic config
- README, .env.example, docker-compose example, requirements

See branch feature/init_project for files and instructions.
